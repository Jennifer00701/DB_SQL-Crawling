VARCHAR 가변 문자열
-> VARCHAR(255) 이렇게 하면 255 바이트! 까지의 데이터를 받겠다
-> VARCHAR = 들어오는 str값에따라 다름


CHAR은 파이썬 str 
-> CHAR(100) 이렇게 컬럼 값을 설정했으면 100자까지 데이터를 받겠다

TEXT 값이 지정되지 않은 문자열

ID -> 고유 번호라  null 을 허용하지 않고 자동 증가해야함.
-> 값 : 숫자 (int) 가장 첫 번째 줄에 작성하는게 좋음 

ID 값을 고유한 값으로 준 후에는 아래 명령어를 넣어줘야함
# 둘 중 하나
primary key(id)
CONSTRAINT crawling_pk PRIMARY KEY(id)

as -> 짧은 단어 변경
beautifulsoup4 as bs4


----- import 메모 

# from bs4 import BeautifulSoup:
이 구문은 BeautifulSoup라는 클래스를 bs4 라이브러리에서 가져오라는 의미입니다. BeautifulSoup는 웹 스크래핑을 위한 파이썬 라이브러리로, 웹 페이지의 복잡한 HTML 코드를 파싱해서 사용자가 원하는 데이터를 쉽게 추출할 수 있도록 도와줍니다.
# from selenium import webdriver:
이 구문은 selenium 라이브러리에서 webdriver라는 모듈을 가져오라는 의미입니다. selenium은 웹 애플리케이션 테스트를 위한 프레임워크입니다. 웹드라이버는 웹 페이지를 제어할 수 있게 해주는 인터페이스로, 브라우저를 직접 제어하여 자동화된 테스트나, 자바스크립트로 렌더링되는 동적인 웹페이지의 데이터를 크롤링할 때 사용됩니다.
# from selenium.webdriver.common.by import By:
이 구문은 selenium.webdriver.common.by 모듈에서 By 클래스를 가져오라는 의미입니다. By 클래스는 웹드라이버를 통해 웹 요소를 찾을 때 사용하는 방법을 정의합니다. 예를 들어, By.ID, By.NAME, By.CLASS_NAME 등으로 웹 요소를 찾을 수 있습니다.
# import time:
이 구문은 파이썬의 표준 라이브러리인 time을 가져오라는 의미입니다. time 모듈은 시간 관련 함수를 제공하며, 여기서는 웹 페이지 로딩 대기나, 간헐적으로 발생하는 행동을 지연시키는 등의 용도로 사용될 수 있습니다.
